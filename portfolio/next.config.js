/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {    
    domains: ['img.btdmp.com', 'image.spreadshirtmedia.net', 'cdn.imgbin.com'],
  },
}

module.exports = nextConfig
